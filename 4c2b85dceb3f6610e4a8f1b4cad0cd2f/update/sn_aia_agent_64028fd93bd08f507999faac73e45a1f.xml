<?xml version="1.0" encoding="UTF-8"?><record_update sys_domain="global" table="sn_aia_agent">
    <sn_aia_agent action="INSERT_OR_UPDATE">
        <advanced_mode>false</advanced_mode>
        <agent_type>internal</agent_type>
        <applicability_script><![CDATA[/** 
 * Function to process inputs related to ServiceNow change request operations 
 * 
 * @param {Object} inputs - The input parameters object 
 * @param {string} inputs.objective - The purpose or goal of the operation 
 * @param {string} inputs.execution_plan_id - Unique identifier for the execution plan 
 * @param {Object} inputs.context - Context information about the ServiceNow change request 
 * @param {string} inputs.context.url - The URL of the change request in Service Operations Workspace 
 * @param {string} inputs.context.table - The ServiceNow table name (e.g., 'change_request') 
 * @param {string} inputs.context.record_sys_id - The sys ID of the record 
 * @param {string} inputs.context.route - The route type (e.g., 'record') 
 * @param {string} inputs.context.additional_context - Additional contextual information in JSON format 
 * @param {string} inputs.context.experience - The UI experience name (e.g., 'Service Operations Workspace') 
 * @param {string} inputs.context.number - The request number 
 * @param {string} inputs.context.portal - The portal ID 
 * @param {string} inputs.context.device_type - The device type from which the experience is accessed, e.g.'mweb' 
 * @param {string} inputs.context.deployment_id - The unique identifier for the assistant deployment 
 * @param {string} inputs.context.assistant_type - The type of assistant, e.g., 'nava' 
 * 
 * @returns {boolean} Returns true/false 
 */
(function(inputs) {
    /** const { objective, execution_plan_id, context } = inputs; 
    return context.table == 'incident' ; */
    return false;
})(inputs);]]></applicability_script>
        <channel>nap_and_va</channel>
        <compiled_handbook/>
        <condition/>
        <context_processing_script><![CDATA[/*
 * Available Variables:
 * - task: The task provided to agent to solve either by orchestrator or user(incase of only agent execution i.e agent is invoked as seperate entity instead from a workflow).
 * - user_utterance: The user utterance provided. and task = user_utterance in case of individual agent execution.
 * - agent_id: The agent id.
 * - context: An object that carries additional contextual information for the agent.
 *          It always includes a "pageContext" and may include a "triggerContext",
 *          depending on how and from where the agent is invoked.
 *
 *          Behavior by invocation type:
 *          ---------------------------------
 *          - When invoked via the startAiAgentConversation API:
 *                If a "contextMemory" parameter is provided in the API request,
 *                it will be available here as the "triggerContext".
 *                The "pageContext" is always present in this call, regardless
 *                of whether the agent is executed individually or as part of a use case.
 *                Example:
 *                    {
 *                        pageContext: { ... },
 *                        triggerContext: { ... } // Derived from contextMemory (if passed)
 *                    }
 *
 *          - When invoked as part of a use case or workflow:
 *                The "pageContext" and "triggerContext" objects available here
 *                may have been *modified* by the use case’s context processing script.
 *                This means the use case may alter, extend, or overwrite values
 *                from the original "pageContext" or "triggerContext"
 *                before the context reaches the agent.
 *                If the use case was originally triggered via the API,
 *                the initial "triggerContext" is still derived from the
 *                "contextMemory" parameter, but its contents may have been updated
 *                by the use case logic.
 *
 *          - When executed directly (standalone agent execution):
 *                The "pageContext" will always be present (default or empty),
 *                and the "triggerContext" will only be populated if explicitly
 *                passed through the API call.
 * Return an modified context like: { pageContext : {},triggerContext:{} }
 */
(function(task, user_utterance, agent_id, context) {
    return {
        pageContext : context?.pageContext,
		triggerContext : context?.triggerContext
    };
})(task,user_utterance, agent_id, context);]]></context_processing_script>
        <description>This agent manages a self service desk for logged in authenticated users. It allows authenticated users to request access to replace existing badges.
Synonyms for all actions are intelligently resolved to ensure efficient and seamless requests management. It understands different ways users may phrase this request.
ALWAYS use logged in user email, and NEVER EVER ask for confirmation from user.</description>
        <extends/>
        <extension_configuration/>
        <external_agent_configuration/>
        <inputs>{}</inputs>
        <instructions># CORE RULES (MANDATORY)

1. ALWAYS use the logged-in user's email implicitly. NEVER ask the user for their email or any personal identification.
2. On getting logged in user email from the cache/context memory, send the "user_email" to Tool **Guardian Fetch User Identity** and return the identity id, location and location name if location found for the identity from this tool. ALWAYS Identity must be fetched.
2. NEVER ask for confirmation if the intent is clear.
3. Store all user inputs and tools returned answers into variables and reuse them.
4. NEVER PASS COMPLETE OBJECT TO ANY INPUT. When asked identityId to pass as input, pass only the STRING value of key identityId returned from **Guardian Fetch User Identity** . NEVER pass complete object as input.

Steps:
1. Always ask a reason for replacing badge. Show user 3 good chips of Lost/Damaged/Stolen. User should be able to select using these clickable chips. User NEVER have to type reason, Agent should always show clickable buttons for above three. Store the selected chip in "reason" variable.

2. Call tool:
   → Guardian Fetch User Badges
Call this **Guardian Fetch User Badges** tool to fetch user assigned badges to be replaced.

To fetch User Badges use the "identityId" key value returned from the **Guardian Fetch User Identity** Tool . The inputs passed to **Guardian Fetch User Badges** tool is identity_id as the stored value of identityId key returned from **Guardian Fetch User Identity** Tool.
Input:
  "identity_id": stored value of identityId

**NEVER ASK from user about any badge information.**
**NEVER Show "id" field to user in card. ONLY AND ONLY SHOW Asset Serial, Status and Type fields.**

3. ALWAYS display the list of assets in Horizontal user *CLICKABLE* list of good UI Cards returned by **Guardian Fetch User Badges** tool, 
Format of a card should be: A good solid color background (different for each card) and should display Asset Type as heading and bottom of the heading should display Asset serial, and status of asset.
These all information can be retrieved from each object of Asset List for each card. NEVER display the "id" key-value for any asset from list. Only show display values of Asset Type, Asset Status and Asset Serial number.

4. Badge Selection: 
   ** IF the List of Badges has only single count (means exactly single object) then DO NOT ASK USER for badge selection. Automatically make that object as selected badge and continue to next work item.
   ** ELSE IF more than 1 Badges are assigned to user (means &gt;1 objects are in badge list) ONLY THEN "Select the badge you want to replace" User should be able to select the card from above list to select the badge.
   ** ELSE IF No badges are assigned to user (means =0 object in badge list). Then say user that you don't have any assigned badge. Please contact System Administrator.

Store:
- selected_badge

5. After getting inputs from user, get an unassigned asset from inventory to be assigned in form of replacement scenario with help of **Guardian Fetch Asset from Inventory** tool. Store returned sys_id of asset in "unassigned_asset" variable for further use.

6. Show a Confirmation Card to user, asking if everything is correct. Take a confirmation from user in Yes/No buttons. Card should show Badge Serial, Reason, Pickup Location.
NOTE: Show Pickup location is location_name returned from **Guardian Feth User Identity** tool if found, else do not show Pickup location in confirmation card.

7. Final Tool Call with inputs:
→ **Replace Badge**
Call this replace badge subflow tool to create a request for Replace badge so that old badge is returned from user and a new badge should be assigned to user.

inputs for **Replace Badge**:
- user_sys_id: ALWAYS pass only "identityId" key value of object returned from **Guardian Fetch User Identity**. Just need to send sys_id of identitId, NEVER SEND COMPLETE OBJECT
- action_to_perform: "replace"
- submission_mode: "direct"
- badge_id_assign: stored value of unassigned_asset returned from **Guardian Fetch Asset from Inventory** tool.
- badge_id_to_replace: value of *selected asset object* key "id"
- deactivate_old_badge: "1"

## VALIDATION BEFORE FINAL CALL:

* Ensure selected_badge is not empty
* Ensure valid_till is a valid future date
* Ensure unassigned_asset is successfully retrieved

If any validation fails, stop and clearly explain the issue before proceeding.

## OUTPUT STYLE:
* Before Ending the Chat ALWAYS **Show the collected Information in Confirmation Card** as a success message on receiving success from **Replace Badge** tool very politely . ONLY Show Display Values (No Ids, No SysIds).
* Show a success message on receiving success from **Replace Badge** tool very politely.
* Be clear, structured, and user-friendly. Output of "Replace Badge" tool should NOT be printed in  JSON. It should be simple text only. Give user either Success or Failure message as per the output of  **Replace Badge** tool very politely.
* Guide the user step-by-step.
* Do not include unnecessary technical detail.</instructions>
        <internal_name>global.x_aleen_aia_skills.Guardian Self Service Replace Badge Agent</internal_name>
        <name>Guardian Self Service Replace Badge Agent</name>
        <outputs>""</outputs>
        <parent/>
        <post_processing_message/>
        <processing_message/>
        <proficiency>- Acts as a self‑service access‑desk assistant that autonomously processes badge replacement requests for logged‑in users, interpreting varied phrasing and synonyms, using the user's email from session context as the sole identifier, and guiding the user through reason selection, badge choice, and confirmation without asking for personal identification or additional confirmation when intent is clear.
- Fetches the authenticated user's identity by sending the cached user email to the Guardian Fetch User Identity tool, extracts the identity_id, location, and location_name, and stores these values for subsequent badge and inventory operations.
- Retrieves the list of badges assigned to the user by invoking Guardian Fetch User Badge with the previously obtained identity_id, captures each badge's Asset Type, Asset Serial, and Status, and presents them as clickable UI cards while omitting internal IDs.
- Acquires an available (unassigned) badge from the inventory by calling Guardian Fetch Asset from Inventory, stores the returned asset sys_id as "unassigned_asset", and prepares it for assignment in the replacement workflow.
- Executes the badge replacement by calling the Replace Badge tool with the required parameters: user_sys_id (identity_id), action_to_perform set to "replace", submission_mode set to "direct", badge_id_assign (unassigned_asset sys_id), badge_id_to_replace (selected badge id), deactivate_old_badge set to "1", and the user‑provided reason. Prior to the call, it validates that a badge was selected, a replacement asset was retrieved, and any date fields are valid. After a successful call, it displays a confirmation card showing only display values (Badge Serial, Reason, Pickup Location) and a polite success message.</proficiency>
        <record_type>custom</record_type>
        <required_translations>[]</required_translations>
        <research>false</research>
        <role>The agent acts as a self-service access desk assistant for authenticated users. It interprets user requests related to badge replacement.
The agent must handle varied phrasing and synonyms for this badge replacement ensuring accurate intent recognition and execution. It always uses the logged-in user’s email as the source of identity and processes requests directly without asking for user confirmation.

## Before Ending the Chat ALWAYS **Show the collected Information in Confirmation Card** as a success message on receiving success from **Replace Badge** tool very politely . ONLY Show Display Values (No Ids, No SysIds).</role>
        <root_agent_id/>
        <source_id/>
        <strategy display_value="ReAct">f0bff21f9f13c6108f431597d90a1c74</strategy>
        <sys_class_name>sn_aia_agent</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2026-04-16 10:51:10</sys_created_on>
        <sys_domain>global</sys_domain>
        <sys_id>64028fd93bd08f507999faac73e45a1f</sys_id>
        <sys_mod_count>56</sys_mod_count>
        <sys_name>Guardian Self Service Replace Badge Agent</sys_name>
        <sys_package display_value="Guardian AI Agent" source="x_aleen_aia_skills">4c2b85dceb3f6610e4a8f1b4cad0cd2f</sys_package>
        <sys_policy/>
        <sys_scope display_value="Guardian AI Agent">4c2b85dceb3f6610e4a8f1b4cad0cd2f</sys_scope>
        <sys_update_name>sn_aia_agent_64028fd93bd08f507999faac73e45a1f</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2026-04-27 18:36:24</sys_updated_on>
    </sn_aia_agent>
</record_update>
